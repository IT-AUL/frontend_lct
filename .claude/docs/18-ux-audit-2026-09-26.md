# 18 · Аудит интерфейса пятью ролями (26.09.2026)

Стенд: mock-режим (записанные ответы реального бэка), все 19 экранов в светлой и тёмной теме, 1440×900 и 1280.
Скриншоты и артефакты агентов — в scratchpad облачной сессии (не в репо). Живой прод из облака недоступен.

## Оценки

| Роль | Оценка | Главное в одной фразе |
|---|---|---|
| Продакт | 6,5/10 | UI сильный, но вторая половина пути громко показывает слабости бэка; финал «0 критичных» недостижим |
| UI/UX-дизайнер | 6,5/10 | Близко к макету; аудит — ключевой экран — ломается на 1280 и тонет в 108 карточках |
| Обычный пользователь | 4/10 | Быстро, но не верю результату: демо игнорирует мой текст, «108 проблем», жаргон |
| Критик (жюри VK) | 3–6/10 по критериям | Красиво показывает слабый результат: прогон без LLM, 0 графиков, нет HTML, текст шаблона в колоде |
| Senior frontend | 7,5/10 | Аккуратный код; pdf.js в главном чанке, опрос умирает на ошибке сети, стрелки ломают клавиатуру |

## Сквозные проблемы (отмечены 3+ ролями)

| # | Проблема | Кто | Суть решения |
|---|---|---|---|
| A | **Финал истории не сходится.** Цепочка «Повторный аудит: 0 критичных» не загорается (бэк не чинит `font_scale`/`font_floor`/`overflow`); в шапке «Готово · 3 варианта» при баннере «осталось 52 критических» | PM, дизайнер, критик, пользователь | Критерий — **0 блокеров** + дельта «ошибок 54 → N»; шапка на аудите/экспорте — «Ждёт решения: N критич.» (как в макете, стр. 1124) |
| B | **108 карточек подряд** (дубли «Контраст 3.23:1» ×3, ~50 ложных `font_scale`), скролл 8790px | все 4 UX-роли | По умолчанию группировать **по правилу** со счётчиком и групповым выбором; в шапке «108 находок · 9 правил · 54 исправимо»; критичные первыми |
| C | **Сырой текст бэка в карточке**: «Google Shape;730;p57», «73.2 при норме 13» без единиц, английский evidence, `text.overflow`, `audit-basic/0.1.0` | PM, дизайнер, пользователь | Человеческая фраза + единицы из словаря правил; техника — в `<details>` «Технические детали» |
| D | **Демо-данные без LLM**: план `deterministic`, «Промпты не использовались», слайды с «product»/«Текст», 0 графиков, N-проверки не запускались | критик, PM, пользователь | Перезаписать фикстуры на прогоне с моделью и контентом с числами; живое демо — с сессией Qwen3-32B; в mock — плашка «Демо: результат записан заранее» |
| E | **HTML-экспорт спрятан**, а доки обещают «карточку Скоро» | критик, PM | Вернуть честную карточку HTML; синхронизировать `ARCHITECTURE.md:250`, `docs/REQUIREMENTS.md:41`, VARIANTS, число тестов |
| F | **Превью после repair устаревшее**: рамки r2 поверх картинки r1; экспорт отдаёт r1 «не исправлено 108» | дизайнер, пользователь, PM | Рамки пунктиром + «превью устарело»; на экспорте предупреждение «открыто N ошибок»; одна кнопка «Скачать итог» по последней ревизии |

## Frontend — делаем сами (по приоритету)

| P | Что | Где | Оценка |
|---|---|---|---|
| P0 | Шапка аудита вылезает за край на 1280 — «Экспорт» недоступен | `widgets/audit-workspace/ui/AuditSummary.module.css` (`min-width:0`, иконки у второстепенных) | 30 мин |
| P0 | Достижимый финал: «0 блокеров» + дельта, статус «Ждёт решения: N» | `widgets/project-rail/model/progress.ts:112`, `ReauditBanner.tsx`, `app/layouts/runStatus.ts` + тесты | 1 ч |
| P0 | Группировка аудита по правилу, схлопывание дублей, групповой выбор | `widgets/audit-workspace`, `entities/audit/lib` | 2–3 ч |
| P1 | Опрос умирает на ошибке сети → экран генерации завис | `entities/generation/api/generationApi.ts:20`, `entities/variant/api/variantApi.ts:23`, `model/phase.ts:28` (backoff + плашка «нет связи») | 1 ч |
| P1 | pdf.js в главном чанке (960 → 465 КБ) | `shared/lib/pdf/pdf.ts` — `import()` внутри `loadPdf` | 30 мин |
| P1 | Карточка проблемы: единицы, техника в `<details>`, место под чекбокс всегда | `widgets/audit-workspace/ui/IssueCard.tsx` | 1–2 ч |
| P1 | Стрелки на `window` перехватывают клавиатуру у кнопок/радио | `AuditWorkspace.tsx:201-228` | 20 мин |
| P1 | Рамки на устаревшем превью: пунктир + пометка; неактивные рамки `opacity .35` без подписей | `widgets/slide-inspector` | 1 ч |
| P1 | Экспорт: предупреждение «открыто N ошибок», HTML-карточка с честным статусом | `pages/export`, `features/export-deck` | 1 ч |
| P1 | План: главная кнопка «К вариантам →», «Пересобрать» — второстепенная с подтверждением | `pages/plan` | 20 мин |
| P1 | Генерация: неактивная «Отменить» пока нет id; этап конвейера подсвечивать | `pages/generation/ui/GenerationPage.tsx:127`, `PipelineStages.tsx` | 40 мин |
| P1 | Сравнение по слайдам выравнивает по индексу → строки сдвинуты | `widgets/variant-compare/lib/rows.ts` (по `slide_plan_id`/разделу) | 1 ч |
| P1 | Плашка «Демо: результат записан заранее» в mock-режиме | `app/layouts/AppFrame.tsx` | 20 мин |
| P2 | 404 `/audit/rules` без проверки capability | `entities/audit/api/ruleCatalogApi.ts:8` | 15 мин |
| P2 | Утечка PDFDocumentProxy (нет `destroy()`) | `shared/lib/pdf/pdf.ts:7-16` | 30 мин |
| P2 | a11y: `aria-label` «Провайдер моделей» при тексте «Модели»; `role=img` скрывает кнопки bbox; тосты ошибок пропадают за 2,6 с; зоны клика 16–18px; контраст подписи активного шага 4,36:1 | `AppHeader.tsx:53`, `PdfPage.tsx:88`, `ToastProvider.tsx`, `Checkbox`, `ProjectRail.module.css` | 1 ч |
| P2 | Мелочи: `+` иконкой в `StepperInput`, `gap:8px` у `.primaryLink`, белый сегмент в полосе цветов ДНК, плейсхолдеры моноширинным шрифтом, разный кегль полей брифа, `sourcemap: 'hidden'` | разные | 1 ч |
| P2 | Тесты на `audit-workspace` (клавиатура, repair-flow), «опрос возобновляется после 502» | — | 1–2 ч |

## Бэкенд / демо — нужно от команды и пользователя

1. **Прогон с LLM** (Qwen3-32B) для демо и видео — без него FR-09 и N-часть FR-16 не показаны. Перезаписать фикстуры mock с этого прогона.
2. **Контент с числами** в демо-брифе → хотя бы 1–2 нативных графика в «Визуальном» (иначе убрать «графиками» из описания стратегии).
3. **HTML-экспорт (D5)**, **PDF после repair (B2)**, **шкала кеглей (Q1)**, **автоисправление до показа (Q2)** — из `docs/BACKEND_HANDOFF.md`.
4. Для демо repair выбирать только реально чинящиеся правила: `color_palette`, `aspect_ratio`, `contrast`; `overflow` не выбирать (B6).
5. Завести 3 проекта (VK Tech, WorkSpace, Education) — критерий 3×3; живой прогон начинать с «Новый проект» и шаблона не из датасета.

## Решения для пользователя

- `docs/BACKEND_HANDOFF.md` в публичном `main` перечисляет P0-баги продукта (критик советует убрать или переименовать в roadmap).
- `MODELS.md` и `AUDIT.md` (DOC-03/04) — ссылаться на бэкенд из README или завести у нас.

## Вопросы жюри, к которым готовиться

Где LLM в демо · покажите нативный график и поменяйте значение в PowerPoint · чем варианты отличаются измеримо · почему показываете колоду со 108 проблемами · где HTML · что сделал фронт сверх отображения JSON · где правка плана до вёрстки.
