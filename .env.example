# Dev (Vite): copy to .env.local
BACKEND_URL=http://localhost:8000
VITE_API_MODE=live

# docker compose (read from .env next to docker-compose.yml)
# WEB_PORT=8080
# WEB_BACKEND_URL=http://api:8000
# BACKEND_DIR=../LCT-prezi-2026
# BACKEND_DOCKERFILE=Dockerfile
# API_PORT=8000
